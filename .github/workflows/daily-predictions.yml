name: Previsions quotidiennes et site

on:
  workflow_dispatch:
  schedule:
    - cron: "15 6 * * *"
      timezone: "Europe/Paris"
    - cron: "15 12 * * *"
      timezone: "Europe/Paris"

concurrency:
  group: scorepredict-pages
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    name: Calculer les previsions
    runs-on: windows-latest
    timeout-minutes: 120
    env:
      SCOREPREDICT_BROWSER_HEADLESS: "0"
      SCOREPREDICT_BROWSER_CHANNEL: msedge
      SCOREPREDICT_BROWSER_ATTEMPTS: "3"
      SCOREPREDICT_BROWSER_FAILURE_DIR: inference/output/browser-failures
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
    steps:
      - name: Recuperer le projet
        uses: actions/checkout@v6

      - name: Installer Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements-pipeline.txt
            requirements-web.txt

      - name: Installer Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm

      - name: Installer les dependances
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-pipeline.txt -r requirements-web.txt
          npm ci

      - name: Identifier l'environnement des modeles
        id: model-runtime
        shell: pwsh
        run: |
          $versions = python -c "import sys,numpy,pandas,sklearn,xgboost; print(f'py{sys.version_info.major}{sys.version_info.minor}-np{numpy.__version__}-pd{pandas.__version__}-sk{sklearn.__version__}-xgb{xgboost.__version__}')"
          "versions=$versions" >> $env:GITHUB_OUTPUT

      - name: Restaurer les modeles figes
        uses: actions/cache@v4
        with:
          path: inference/model_cache
          key: scorepredict-models-${{ runner.os }}-${{ steps.model-runtime.outputs.versions }}-${{ hashFiles('inference/portfolio_presets.py', 'inference/upcoming_portfolio_strategy.py', 'train/make_dataset.py', 'train/ml_common.py', 'Data/**/*.csv', 'requirements-pipeline.txt') }}

      - name: Verifier le code
        shell: pwsh
        run: |
          python -m unittest discover -s tests -p "test_*.py"
          if ($LASTEXITCODE -ne 0) { throw "Les tests Python ont échoué." }
          node --check inference/sportytrader_page_text.mjs
          if ($LASTEXITCODE -ne 0) { throw "La syntaxe du lecteur de calendrier est invalide." }
          node --check production/static/assets/app.js
          if ($LASTEXITCODE -ne 0) { throw "La syntaxe du dashboard est invalide." }
          node --check production/static/assets/landing.js
          if ($LASTEXITCODE -ne 0) { throw "La syntaxe de la landing page est invalide." }

      - name: Calculer les 21 prochains jours
        shell: pwsh
        run: |
          $pythonExe = (Get-Command python).Source
          ./production/publish_dashboard.ps1 -PythonExe $pythonExe

      - name: Refuser une publication incomplete
        shell: pwsh
        run: |
          python -m inference.validate_publication_kickoffs `
            --snapshot production/static/data/dashboard.json `
            --fixtures inference/output/sportytrader_upcoming_portfolio_odds.csv
          if ($LASTEXITCODE -ne 0) { throw "Les horaires publics ne correspondent pas au calendrier vérifié." }
          $dashboard = Get-Content -Raw production/static/data/dashboard.json | ConvertFrom-Json
          $expectedPortfolio = python -c "from inference.portfolio_presets import DEFAULT_PORTFOLIO_NAME; print(DEFAULT_PORTFOLIO_NAME)"
          if ($dashboard.meta.status -ne "ready") { throw "Le tableau de bord n'est pas pret." }
          if ($dashboard.predictions.Count -ne $dashboard.summary.upcomingBets) { throw "Le site contient des tendances non recommandees." }
          if (-not $dashboard.quality.teamRegistryReady) { throw "La liste des clubs est incomplete." }
          if ($dashboard.meta.activePortfolio -ne $expectedPortfolio) { throw "Le snapshot ne correspond pas au portefeuille de production." }
          if ($dashboard.quality.protocol.topExperiment -ne "draw_consensus_nonfavorite") { throw "L'audit ne reference pas le champion valide." }

      - name: Preparer GitHub Pages
        uses: actions/configure-pages@v6

      - name: Charger le site valide
        uses: actions/upload-pages-artifact@v5
        with:
          path: production/static

      - name: Conserver les diagnostics en cas d'echec
        if: ${{ failure() }}
        uses: actions/upload-artifact@v5
        with:
          name: scorepredict-diagnostics-${{ github.run_id }}
          path: |
            inference/output/browser-failures
            inference/output/*.csv
            inference/output/*.json
            train/output/*.json
          if-no-files-found: ignore
          retention-days: 7

  deploy:
    name: Publier le site
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Publier sur GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
